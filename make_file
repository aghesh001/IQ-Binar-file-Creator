CC = gcc             # the compiler: gcc for C program, define as g++ for C++
TARGET = iq_binary_creator      # the build target executable:

all: $(TARGET)

  $(TARGET): $(TARGET).c
    $(CC) -o $(TARGET) $(TARGET).c

clean:
  $(RM) $(TARGET)